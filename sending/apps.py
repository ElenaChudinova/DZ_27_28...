from django.apps import AppConfig


class SendingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "sending"

class MessageConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "sending"

class MailingRecipientConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "sending"